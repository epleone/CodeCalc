# 计算器，
基本功能: 支持 1-9， (), +-*/

- 是否是内置常量
- 是否是变量
- 支持min, max
- 判断是否是op
- 判断是否是函数, 
- 这个不好，括号( ... )可以当一个单值恒等函数处理
- 判断是否是 赋值符号 =, +=, -=, *=, /=


# 思路

- 构建抽象语法树(AST)
- 使用表达式树（expression tree）
 https://blog.csdn.net/2303_77100984/article/details/134627768

- 是的，当前的表达式解析确实显得有些复杂。我们可以采用更简单的"调度场算法"(Shunting Yard Algorithm)来重构代码，这是一个经典的中缀表达式解析算法。

- 可视化语法树

- 安装node.js  直接使用js来写吧

- 支持函数
- 支持OP
- 支持一元OP


"""
#TODO
1. 添加前缀操作符 `0b111`,  `0o567`, `0xfff`
2. 添加函数
"""