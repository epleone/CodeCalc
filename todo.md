# TODO
- [ ] 所以函数都进行单元测试，GitHub action 测试
- [ ] 快照图标bug, win10?
- [ ] 支持拖动
- [ ] 计算值和显示值分开
- [ ] 时区支持
    日期和时间的显示逻辑在优化一下。
    `> @8`  时区
    `> @-8`
- [ ] 分离窗口后关闭没有历史记录
- [ ] in-place op有bug, 比如 a += 123, a会做多次计算，而不是预期的一次, a = a + 1也会

自定义函数添加test, 添加一个测试函数添加自定义函数
f(12345) f("12345") f(#1d)  f([1 2 3 4 5])
a = 12345 a = "12345" a = #1d  a = [ 1 2 3 4 5] a = {1 2 3 4 5}
f(a)

支持进制转换
“大写人民币”这个插件的大部分使用场景应该会与财务工作有关，可参考财政部发布的《会计基础工作规范》中对大写金额的书写要求。

## RoadMap 
### V3.0
**界面改进**
- 设置持久化保存，支持设置输出格式，小数位数，科学计数法，十六进制

### V3.2
**自定义函数**
-? 添加性能监控, 计算超时返回 throw new Error('计算超时');

### V3.3
to be determined