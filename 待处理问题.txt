1. (123).unknown  不报错：语法树解析出错，报错
2. 如何判断解析树正确
3. 括号内赋值 ((x=1))  预处理检查？ 多个等号不支持 -- 不用改了
4. 1e2e3，1e2.3 不报错， 转换时处理，报错
5. 后缀前缀混用 (1°°)   预处理检查, 报警告
6. 赋值运算符连用 (x = y = 1)  -- 不用改了。特性：y = (x = 1 + 2 ) **(4)
7. max('1', 2)  log('1')  不该接受字符串，-- 不用改了, 这是js本身的问题！


== 
如何实现在func里用其它已经定义的函数？

已处理
1. 乘号`x` 单独处理，不要写到属性里了。 逻辑解决
